Image 组件是一个增强的图片显示组件，支持两种加载方式：

1. 通过 src 属性直接加载图片URL
2. 通过 id 属性从 OSS 服务器加载图片

组件特性：
- 自动加载状态显示和错误处理
- 支持 Avatar 头像模式，可显示默认性别图标
- 支持自定义加载状态和错误状态组件
- 完全兼容原生 img 标签的基本属性

主要应用场景：
- 用户头像展示
- 商品图片展示
- 文档预览
- 需要加载状态的图片显示
